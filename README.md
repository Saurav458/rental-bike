note-: pages are full screen which has some routes

note-: layout never chnage only page chnage are just componet or elments which use by pages

note-: layouts are something which will show layout and pages

note-: AuthLayout no need to show layout like Navbar, footer